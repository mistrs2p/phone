{
  "name": "phonebook-final",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "build": "rimraf ./build && tsc && npm run copy-files",
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "npm run build && node build/main.js ",
    "copy-files": "copyfiles -u 1 src/**/*.{sql,json} build/",
    "start:dev": "npx nodemon"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "@types/node": "^20.14.12",
    "@types/sqlite3": "^3.1.11",
    "nodemon": "^3.1.0",
    "rimraf": "^5.0.5",
    "ts-node": "^10.9.2",
    "typescript": "^5.4.5"
  },
  "dependencies": {
    "commander": "^12.1.0",
    "copyfiles": "^2.4.1",
    "dotenv": "^16.4.5",
    "mysql2": "^3.11.0",
    "sqlite3": "^5.1.7"
  }
}
